/**
 * Pescheck API
 * Official client library for the Pescheck API (v2), generated from the OpenAPI specification. Authenticate with OAuth2 client credentials and use the checks, profiles, screenings, webhooks and divisions endpoints. See https://github.com/pescheckit/pescheck-clients for installation and usage.
 *
 * The version of the OpenAPI document: 2.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The V2ScreeningNote model module.
 * @module model/V2ScreeningNote
 * @version 0.1.0
 */
class V2ScreeningNote {
    /**
     * Constructs a new <code>V2ScreeningNote</code>.
     * A note recorded on the screening. &#x60;check_id&#x60; is the Check the note targets (null for type-only or unscoped notes).
     * @alias module:model/V2ScreeningNote
     * @param note {String} 
     * @param checkType {String} 
     * @param checkId {String} 
     * @param createdAt {Date} 
     */
    constructor(note, checkType, checkId, createdAt) { 
        
        V2ScreeningNote.initialize(this, note, checkType, checkId, createdAt);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, note, checkType, checkId, createdAt) { 
        obj['note'] = note;
        obj['check_type'] = checkType;
        obj['check_id'] = checkId;
        obj['created_at'] = createdAt;
    }

    /**
     * Constructs a <code>V2ScreeningNote</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/V2ScreeningNote} obj Optional instance to populate.
     * @return {module:model/V2ScreeningNote} The populated <code>V2ScreeningNote</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new V2ScreeningNote();

            if (data.hasOwnProperty('note')) {
                obj['note'] = ApiClient.convertToType(data['note'], 'String');
            }
            if (data.hasOwnProperty('check_type')) {
                obj['check_type'] = ApiClient.convertToType(data['check_type'], 'String');
            }
            if (data.hasOwnProperty('check_id')) {
                obj['check_id'] = ApiClient.convertToType(data['check_id'], 'String');
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>V2ScreeningNote</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>V2ScreeningNote</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of V2ScreeningNote.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['note'] && !(typeof data['note'] === 'string' || data['note'] instanceof String)) {
            throw new Error("Expected the field `note` to be a primitive type in the JSON string but got " + data['note']);
        }
        // ensure the json data is a string
        if (data['check_type'] && !(typeof data['check_type'] === 'string' || data['check_type'] instanceof String)) {
            throw new Error("Expected the field `check_type` to be a primitive type in the JSON string but got " + data['check_type']);
        }
        // ensure the json data is a string
        if (data['check_id'] && !(typeof data['check_id'] === 'string' || data['check_id'] instanceof String)) {
            throw new Error("Expected the field `check_id` to be a primitive type in the JSON string but got " + data['check_id']);
        }

        return true;
    }


}

V2ScreeningNote.RequiredProperties = ["note", "check_type", "check_id", "created_at"];

/**
 * @member {String} note
 */
V2ScreeningNote.prototype['note'] = undefined;

/**
 * @member {String} check_type
 */
V2ScreeningNote.prototype['check_type'] = undefined;

/**
 * @member {String} check_id
 */
V2ScreeningNote.prototype['check_id'] = undefined;

/**
 * @member {Date} created_at
 */
V2ScreeningNote.prototype['created_at'] = undefined;






export default V2ScreeningNote;

