/**
 * Pescheck API
 * API for Pescheck  ## Authentication This API supports three authentication methods: 1. **OAuth2** (recommended for API clients) - Use client credentials flow 2. **JWT Bearer** (for user-based access) - Obtain tokens via /api/jwt/ endpoint 3. **Cookie** (for browser-based access) - Session authentication via web login  ## Quick Start for API Clients 1. Select your environment from the servers dropdown 2. Use OAuth2 with client credentials flow 3. Scopes are automatically included: read:api, create:api, update:api
 *
 * The version of the OpenAPI document: 2.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import DivisionReadOnly from './DivisionReadOnly';

/**
 * The PaginatedDivisionReadOnlyList model module.
 * @module model/PaginatedDivisionReadOnlyList
 * @version 0.0.1
 */
class PaginatedDivisionReadOnlyList {
    /**
     * Constructs a new <code>PaginatedDivisionReadOnlyList</code>.
     * @alias module:model/PaginatedDivisionReadOnlyList
     * @param count {Number} 
     * @param next {String} 
     * @param previous {String} 
     * @param results {Array.<module:model/DivisionReadOnly>} 
     */
    constructor(count, next, previous, results) { 
        
        PaginatedDivisionReadOnlyList.initialize(this, count, next, previous, results);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, count, next, previous, results) { 
        obj['count'] = count;
        obj['next'] = next;
        obj['previous'] = previous;
        obj['results'] = results;
    }

    /**
     * Constructs a <code>PaginatedDivisionReadOnlyList</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PaginatedDivisionReadOnlyList} obj Optional instance to populate.
     * @return {module:model/PaginatedDivisionReadOnlyList} The populated <code>PaginatedDivisionReadOnlyList</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PaginatedDivisionReadOnlyList();

            if (data.hasOwnProperty('count')) {
                obj['count'] = ApiClient.convertToType(data['count'], 'Number');
            }
            if (data.hasOwnProperty('next')) {
                obj['next'] = ApiClient.convertToType(data['next'], 'String');
            }
            if (data.hasOwnProperty('previous')) {
                obj['previous'] = ApiClient.convertToType(data['previous'], 'String');
            }
            if (data.hasOwnProperty('results')) {
                obj['results'] = ApiClient.convertToType(data['results'], [DivisionReadOnly]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PaginatedDivisionReadOnlyList</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PaginatedDivisionReadOnlyList</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PaginatedDivisionReadOnlyList.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['next'] && !(typeof data['next'] === 'string' || data['next'] instanceof String)) {
            throw new Error("Expected the field `next` to be a primitive type in the JSON string but got " + data['next']);
        }
        // ensure the json data is a string
        if (data['previous'] && !(typeof data['previous'] === 'string' || data['previous'] instanceof String)) {
            throw new Error("Expected the field `previous` to be a primitive type in the JSON string but got " + data['previous']);
        }
        if (data['results']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['results'])) {
                throw new Error("Expected the field `results` to be an array in the JSON data but got " + data['results']);
            }
            // validate the optional field `results` (array)
            for (const item of data['results']) {
                DivisionReadOnly.validateJSON(item);
            };
        }

        return true;
    }


}

PaginatedDivisionReadOnlyList.RequiredProperties = ["count", "next", "previous", "results"];

/**
 * @member {Number} count
 */
PaginatedDivisionReadOnlyList.prototype['count'] = undefined;

/**
 * @member {String} next
 */
PaginatedDivisionReadOnlyList.prototype['next'] = undefined;

/**
 * @member {String} previous
 */
PaginatedDivisionReadOnlyList.prototype['previous'] = undefined;

/**
 * @member {Array.<module:model/DivisionReadOnly>} results
 */
PaginatedDivisionReadOnlyList.prototype['results'] = undefined;






export default PaginatedDivisionReadOnlyList;

